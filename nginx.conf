server {
    listen 3000;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    access_log off;
    error_log /dev/stderr warn;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
